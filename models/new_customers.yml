version: 2

models:
  - name: new_customers
    description: > 
      Count of customers with first ever order for each calendar month

    columns:
      - name: month
        description: calendar month
        tests:
          - unique
          - not_null
      - name: new_customers
        description: total number of new customers that month